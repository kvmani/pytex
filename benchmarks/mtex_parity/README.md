# MTEX Parity Benchmarks

This directory is reserved for benchmark inputs, manifests, and result notes used to trace PyTex behavior against MTEX public tests and documented examples.

The authoritative ledger is `docs/testing/mtex_parity_matrix.md`.
