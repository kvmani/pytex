# MTEX Generator Scripts

These MATLAB/MTEX scripts define the intended regeneration entrypoints for the pinned parity fixtures.

The current repository checks in small deterministic fixtures directly. When MTEX regeneration is available, these scripts should emit the JSON fixture bundles under `fixtures/mtex_parity/` without changing their schema.
