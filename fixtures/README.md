# Fixtures

This directory contains canonical datasets and placeholders used by tests, examples, and validation workflows.

The initial scaffold focuses on semantic fixtures, not large experimental datasets. As algorithms land, fixture manifests should be added here with explicit provenance and usage notes.
